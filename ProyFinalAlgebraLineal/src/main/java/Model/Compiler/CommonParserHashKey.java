package Model.Compiler;

/**
 * Alocate an enum with all values from a common hashmap of Array<String> for Parser Model
 */
public enum CommonParserHashKey {
    INVERSE, DETERMINANT, RANK, ENCRYPT, DECRYPT, GAUSS, JORDAN, MARKOV, ARITH_MATRIX, VECTORS, ARITH_VECTOR
}
